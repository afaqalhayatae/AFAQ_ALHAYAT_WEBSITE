Act as `AGT-ORCH`.

Prepare the next reversible repository-stabilization job. Read
`00_GOVERNANCE/AGENT_REGISTRY.md`,
`00_GOVERNANCE/AGENT_APPROVAL_WORKFLOW.md`, and
`08_DIGITAL_SYSTEMS/AUTOMATION/AGENT_RUNBOOK.md`.

First inspect Git state and define the bounded job envelope. Do not modify files
until the scope, allowed write paths, validation, and rollback reference are
clear.

Never delete, irreversibly migrate, change canonical business facts, stage,
commit, or push without the exact required approval.

Complete safe `A0`/`A1` work autonomously. Show the owner only a material
decision, an `A4` gate, a blocker, or the final phase acceptance summary.
